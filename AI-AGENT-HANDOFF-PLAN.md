# GrowthPilot - AI Agent Handoff & Execution Plan (Operational & QA Focus)

> **Context:** This plan is generated by Gemini CLI based on the state of the local workspace `D:\subject01`, the `HANDOFF.md` document, and successful local test/build runs on 2026-04-12. The project is a stable, deployable MVP.
> **Role:** You are the executing AI agent. You must act as a precise implementer of the steps below, assuming the role of a DevOps engineer and QA tester. Your goal is to secure the deployment pipeline and verify the core loop before adding any new features.

## 1. Project Status Summary

- **Codebase:** Stable MVP. Next.js 16 App Router, React 19, TypeScript, Cloudflare D1/Drizzle, Tailwind CSS.
- **Verification:** Local `test:unit`, `test:component`, and `build` commands pass successfully. E2E tests have been removed intentionally.
- **Git State:** Local changes exist (modifications to `package.json`, `.gitignore`, `README.md`, and deletion of old `.spec.ts` files). The latest commit is `chore: add github-to-cloudflare auto deploy workflow`.
- **Pending Blockers:**
  1. Unpushed local commits and staged/unstaged changes due to network instability on the local machine.
  2. Missing GitHub Secrets (`CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`) required for the deployment workflow.
  3. Final manual QA of the deployed Cloudflare Worker.

## 2. Execution Directives (Strict Order)

You must execute the following phases sequentially. Do not proceed to the next phase until the current one is fully resolved and verified.

### Phase 1: Git Stabilization & Push

The local repository has pending changes and commits that haven't reached GitHub.

**Steps:**
1. **Review Local Changes:** Review the uncommitted changes (`git status`, `git diff`). The current changes involve cleaning up old testing files (Playwright, `tests/e2e/`), updating `.gitignore`, and modifying `README.md`. These are valid cleanup steps aligned with `HANDOFF.md`.
2. **Commit Cleanup:** Stage and commit the remaining cleanup changes.
   - Suggested commit message: `chore: final cleanup of e2e testing artifacts and update docs for handoff`
3. **Resolve Push Blocker:** The `HANDOFF.md` notes a `443` network issue when pushing from the user's machine.
   - **Action:** Attempt to push the changes (`git push origin main`).
   - **Contingency:** If the push fails due to network issues, you must inform the user and assist them in setting up SSH authentication for Git or configuring a proxy. **Do not proceed to Phase 2 until the code is successfully pushed to the remote repository.**

### Phase 2: CI/CD Pipeline Configuration

The code must be deployable via GitHub Actions.

**Steps:**
1. **Instruct the User:** You cannot configure GitHub Secrets directly via CLI tools on the user's local machine. You MUST instruct the user to manually add the following secrets to their GitHub repository (`Settings -> Secrets and variables -> Actions`):
   - `CLOUDFLARE_API_TOKEN`
   - `CLOUDFLARE_ACCOUNT_ID`
2. **Wait for Confirmation:** Explicitly ask the user to confirm once these secrets have been added.

### Phase 3: Deployment Verification

Once secrets are configured, a deployment must be triggered and verified.

**Steps:**
1. **Trigger Workflow:** Instruct the user to trigger the GitHub Action manually, or trigger it yourself by making an empty commit (`git commit --allow-empty -m "trigger: deployment"`) and pushing it.
2. **Monitor Build:** Ask the user to verify the Action runs successfully in the GitHub UI. It should run tests, build the Next.js app, migrate the remote D1 database, and deploy to Cloudflare.
3. **Retrieve URL:** Obtain the final production URL from the user or the deployment logs.

### Phase 4: Manual QA Protocol

The automated E2E tests were removed. You must guide the user through a structured manual QA process using the live production URL.

**Protocol:** Ask the user to perform the following steps and report the results for each:

1. **Onboarding Flow (`/onboarding`):**
   - Goal creation works.
   - AI plan generation succeeds (if `DASHSCOPE_API_KEY` is set) or gracefully falls back to the rule-based template.
2. **Dashboard Review (`/dashboard`):**
   - The generated tasks appear correctly.
   - The UI renders smoothly (check for hydration errors or styling glitches on the deployed version).
3. **Execution Loop (`/focus`):**
   - Start a task.
   - Complete the task or log a delay reason.
   - Verify the state updates correctly without errors.
4. **Weekly Review (`/review`):**
   - Check if the completed task or delay reason is reflected in the generated review summary.
5. **PM Console Verification (`/console`):**
   - Confirm that the metrics (completion rate, delay reasons) accurately reflect the actions taken in steps 3 and 4.

## 3. Post-QA Rules

- **If QA Fails:** Diagnose the specific issue using the local environment or remote logs. Fix the bug, commit, push, and restart the QA process from Phase 3.
- **If QA Passes:** The MVP is officially stable. Only then should you entertain discussions about adding new features or enhancing the AI capabilities.